{
  "name": "new-hugo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass:watch": "node-sass --watch assets/sass/main.sass --output static/css/",
    "hugo:serve": "hugo serve",
    "dev": "concurrently \"npm run sass:watch\" \"npm run hugo:serve\"",
    "build": "hugo --gc --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "node-sass": "^8.0.0"
  }
}
